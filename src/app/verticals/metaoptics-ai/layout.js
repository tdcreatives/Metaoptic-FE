import { buildPageMetadataFromSection } from '@/lib/seo';

export const metadata = buildPageMetadataFromSection('metaopticsAi');

export default function MetaopticsAiLayout({ children }) {
    return children;
}
