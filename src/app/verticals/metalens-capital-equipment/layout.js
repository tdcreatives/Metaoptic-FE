import { buildPageMetadataFromSection } from '@/lib/seo';

export const metadata = buildPageMetadataFromSection('metalensCapitalEquipment');

export default function MetalensCapitalEquipmentLayout({ children }) {
    return children;
}
