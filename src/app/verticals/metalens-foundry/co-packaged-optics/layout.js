import { buildPageMetadataFromSection } from '@/lib/seo';

export const metadata = buildPageMetadataFromSection('coPackagedOptics');

export default function CoPackagedOpticsLayout({ children }) {
    return children;
}
