RewriteEngine On

# 1) Serve real files/directories as-is.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2) For extensionless URLs (any depth), serve matching .html file.
# Example: /about-us -> /about-us.html
# Example: /verticals/metalens-products/development-kits -> /verticals/metalens-products/development-kits.html
RewriteCond %{DOCUMENT_ROOT}/$1.html -f
RewriteRule ^(.+?)/?$ /$1.html [L]

# 3) Also support exported folder format with index.html (if present).
# Example: /news/some-slug -> /news/some-slug/index.html
RewriteCond %{DOCUMENT_ROOT}/$1/index.html -f
RewriteRule ^(.+?)/?$ /$1/index.html [L]